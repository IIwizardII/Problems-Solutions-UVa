#include <iostream>
#include <cstdio>
#include <cstring>
#include <string>
using namespace std;

int main(){
    //freopen("checker.txt","r",stdin);
    char msg[1001];
    char rev[1001];
    char tmp[1001];
    int len,i,j,a,b;
    while(gets(msg)){
        strrev(msg);

        cout << msg;

    cout << "\n";
    }

return 0;
}
